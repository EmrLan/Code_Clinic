import { StyleSheet, Text, View } from "react-native";
import AppText from "../components/AppText";

export default function Flights() {
    return <View>
        <AppText variant='title'>Flights</AppText>
    </View>
}

const styles = StyleSheet.create({
    Container: {
    }
})